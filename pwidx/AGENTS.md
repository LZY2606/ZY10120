# 项目约定 (pwidx)

- Go 1.22+，标准库优先；唯一第三方依赖为纯 Go 的 `modernc.org/sqlite`（无需 cgo）。
- 所有物理公式集中在 `internal/diffract`，求解器不得自行重算 d 值。
- 候选排序必须与并行度无关：随机建议在单线程中按种子确定性生成，打分是纯函数。
- 不要为“唯一正确答案”做断言；UI/文档一律使用候选与置信度措辞。
